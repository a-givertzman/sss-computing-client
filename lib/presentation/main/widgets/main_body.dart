import 'package:flutter/material.dart';
///
class MainBody extends StatelessWidget {
  ///
  const MainBody({super.key});
  //
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('[MainPage empty]'),
    );
  }
}
